export type User = { id: string; email: string; full_name: string; role: string };
export type HCP = { id: string; name: string; specialty: string; institution?: string; city?: string; email?: string; phone?: string; sentiment_score: number; last_contacted_at?: string; interaction_count?: number };
export type Interaction = { id: string; hcp_id: string; hcp?: HCP; interaction_type: string; occurred_at: string; attendees: string[]; topics_discussed: string[]; materials_shared: string[]; samples_distributed: string[]; observed_sentiment: string; outcome: string; follow_up_action?: string; summary: string };
export type DashboardMetrics = { total_hcps: number; todays_meetings: number; pending_followups: number; positive_sentiment_pct: number; monthly_interactions: { month: string; meetings: number }[]; sentiment_breakdown?: { name: string; value: number }[]; product_discussions?: { name: string; mentions: number }[]; follow_up_completion?: number; recent_activity: { doctor: string; summary: string; sentiment: string; date: string }[]; upcoming_meetings: { id?: string; doctor: string; time: string; type: string; priority?: string }[] };
export type FollowUp = { id: string; hcp_id: string; interaction_id: string; due_at?: string; priority: string; status: string; action: string; hcp?: HCP };
export type TokenPayload = { access_token: string; refresh_token: string; token_type: string; user: User };
export type AIChatResponse = { intent: string; response: string; tool_used?: string | null; data?: Record<string, unknown>; missing_fields?: string[]; confidence?: number };
